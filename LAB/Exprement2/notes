AIM:

    To Create a user profile page using props.

DESCRIPTION:

    This project is a User Profile Page built with React using props. The main App component passes user details such as name,
    age, email, and bio as props to the UserProfile component. The UserProfile component then displays this data inside a styled
    profile card, making the UI clean and reusable. By using props, the same component can be reused for multiple users with different data. 
    This demonstrates the reusability and modularity of React components.

CODE:

    // App.js
    import React from "react";
    import UserProfile from "./UserProfile";
    
    function App() {
      return (
        <div>
          <h1 style={{ textAlign: "center" }}>User Profile Page</h1>
          <UserProfile 
            name="Mounith D"
            age={21}
            email="mounith@example.com"
            bio="Passionate about technology, agriculture, and building useful web apps."
          />
        </div>
      );
    }
    
    export default App;
    // UserProfile.js
    import React from "react";
    
    function UserProfile(props) {
      return (
        <div style={styles.card}>
          <h2>{props.name}</h2>
          <p><strong>Age:</strong> {props.age}</p>
          <p><strong>Email:</strong> {props.email}</p>
          <p><strong>Bio:</strong> {props.bio}</p>
        </div>
      );
    }
    
    const styles = {
      card: {
        border: "1px solid #ccc",
        borderRadius: "12px",
        padding: "20px",
        margin: "20px auto",
        width: "300px",
        backgroundColor: "#f9f9f9",
        textAlign: "center",
        boxShadow: "0px 4px 8px rgba(0,0,0,0.1)"
      }
    };
    
    export default UserProfile;

RESULT:

    Thus a user profile page using props were created successfully.
